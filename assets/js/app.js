let departments=[];
const fallbackDepartments=[{id:'sales',name:'Vertrieb',config:'config/excel-sales.xml',dataPath:'data/sales/{period}/sales.xlsx'},{id:'production',name:'Produktion',config:'config/excel-production.xml',dataPath:'data/production/{period}/production.xlsx'},{id:'support',name:'Support',config:'config/excel-support.xml',dataPath:'data/support/{period}/support.xlsx'},{id:'logistics-it',name:'Logistik und IT',config:'config/excel-logistics-it.xml',dataPath:'data/logistics-it/{period}/logistics-it.xlsx'},{id:'accounting',name:'Buchhaltung',config:'config/excel-accounting.xml',dataPath:'data/accounting/{period}/accounting.xlsx'},{id:'quality',name:'QM',config:'config/excel-qm.xml',dataPath:'data/quality/{period}/quality.xlsx'},{id:'marketing',name:'Marketing',config:'config/excel-marketing.xml',dataPath:'data/marketing/{period}/marketing.xlsx'}];
const periods=['2026-W25','2026-W24','2026-W23','2026-W22','2026-W21','2026-W20'];
const base={sales:[820,760,715,690,640,610],production:[74,72,70,68,65,63],support:[92,89,87,86,84,81],['logistics-it']:[96,94,91,90,88,86],accounting:[99,98,97,96,95,94],quality:[97,96,95,93,92,91],marketing:[48,45,44,41,39,37]};
let currentDepartment='sales';let chart=null;
const euro=v=>new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(v*1000);
async function init(){departments=await loadDepartments();currentDepartment=location.hash.replace('#','')||departments[0].id;renderNavigation();renderSelects();bindEvents();renderDashboard();window.addEventListener('hashchange',()=>{const id=location.hash.replace('#','');if(departments.some(d=>d.id===id)){currentDepartment=id;document.getElementById('departmentSelect').value=id;renderDashboard();}})}
async function loadDepartments(){try{const res=await fetch('config/departments.xml');if(!res.ok)throw new Error('config not found');const xml=new DOMParser().parseFromString(await res.text(),'application/xml');return [...xml.querySelectorAll('department')].map(x=>({id:x.getAttribute('id'),name:x.getAttribute('name'),config:x.getAttribute('config'),dataPath:x.getAttribute('dataPath')}));}catch(e){console.warn('Fallback departments used',e);return fallbackDepartments;}}
function renderNavigation(){const nav=document.getElementById('departmentNav');nav.innerHTML='<a href="#overview" data-id="'+departments[0].id+'">Uebersicht</a>';departments.forEach(d=>{const a=document.createElement('a');a.href='#'+d.id;a.textContent=d.name;a.dataset.id=d.id;nav.appendChild(a)});}
function renderSelects(){const p=document.getElementById('periodSelect');p.innerHTML='';periods.forEach(x=>{const o=document.createElement('option');o.value=x;o.textContent=x;p.appendChild(o)});const s=document.getElementById('departmentSelect');s.innerHTML='';departments.forEach(d=>{const o=document.createElement('option');o.value=d.id;o.textContent=d.name;s.appendChild(o)});s.value=currentDepartment;}
function bindEvents(){document.getElementById('departmentNav').addEventListener('click',e=>{if(e.target.dataset.id){e.preventDefault();location.hash=e.target.dataset.id;}});document.getElementById('departmentSelect').addEventListener('change',e=>{location.hash=e.target.value;});document.getElementById('periodSelect').addEventListener('change',renderDashboard);document.getElementById('tableSearch').addEventListener('input',filterTable);}
function renderDashboard(){const dep=departments.find(d=>d.id===currentDepartment)||departments[0];const values=base[dep.id]||[0,0,0,0,0,0];const active=document.getElementById('periodSelect').selectedIndex||0;const current=values[active];document.getElementById('pageTitle').textContent=dep.name;document.getElementById('kpiRevenue').textContent=euro(current);document.getElementById('kpiCosts').textContent=euro(Math.round(current*.68));document.getElementById('kpiResult').textContent=euro(Math.round(current*.32));document.getElementById('kpiTarget').textContent=Math.min(119,Math.round(90+current/30))+'%';renderCards();renderTable(current,dep);renderChart(dep.name,values);}
function renderCards(){const box=document.getElementById('departmentCards');box.innerHTML='';departments.forEach(d=>{const div=document.createElement('div');div.className='dept-card';div.innerHTML='<strong>'+d.name+'</strong><small>'+d.config+'</small>';box.appendChild(div);});}
function renderTable(v,dep){const rows=[['Datenpfad',dep.dataPath.replace('{period}',document.getElementById('periodSelect').value),'','Info'],['Umsatz',euro(v),euro(Math.round(v*.94)),'gruen'],['Kosten',euro(Math.round(v*.68)),euro(Math.round(v*.70)),'gelb'],['Ergebnis',euro(Math.round(v*.32)),euro(Math.round(v*.24)),'gruen'],['Planerfuellung',Math.min(119,Math.round(90+v/30))+'%','96%','gruen']];document.querySelector('#dataTable tbody').innerHTML=rows.map(r=>'<tr><td>'+r[0]+'</td><td>'+r[1]+'</td><td>'+r[2]+'</td><td>'+r[3]+'</td></tr>').join('');}
function renderChart(label,values){const ctx=document.getElementById('trendChart');if(!window.Chart||!ctx)return;if(chart)chart.destroy();chart=new Chart(ctx,{type:'line',data:{labels:periods,datasets:[{label:label,data:values,tension:.35,fill:false}]},options:{responsive:true,plugins:{legend:{display:false}}}});}
function filterTable(e){const q=e.target.value.toLowerCase();document.querySelectorAll('#dataTable tbody tr').forEach(r=>{r.style.display=r.textContent.toLowerCase().includes(q)?'':'none'});}
document.addEventListener('DOMContentLoaded',init);
