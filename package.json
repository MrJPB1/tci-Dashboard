{
  "name": "tci-controlling-dashboard",
  "version": "0.9.0-rc1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js && node scripts/validate-config.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
